# 需要手動驗證的項目

自 2026-09-09 起，開發以功能進度為主：**只寫單元測試**，互動／瀏覽器層面不好自動化的
就跳過，改列在這裡讓擁有者手動驗。

每一列都寫清楚「怎麼操作」與「該看到什麼」，才不會變成一句無法執行的空話。

狀態：`待驗` / `通過` / `有問題`

---

## 怎麼開始跑

### 1. 桌機瀏覽器（純本機模式，不用 Google 帳號）

```bash
npm run dev          # http://localhost:5173
```

沒設定 `VITE_GOOGLE_CLIENT_ID` 時 App 以純本機模式運作：資料存 IndexedDB、
不跳登入、不同步。**D／E／S／C 四組（日常頁、記一筆、統計、分類）全部可以在
這個模式下驗完**，不需要任何 Google 設定。

用瀏覽器的裝置模擬（F12 → 切換裝置工具列）挑 iPhone 12 Pro 390×844、
iPhone 14 Pro Max 430×932、iPhone SE 375×667 三個尺寸，V1–V9 的版面項就是在
這裡量。量法：在 Console 裡跑

```js
// V1：所有可點元素 ≥44px
[...document.querySelectorAll('button,[role=button]')]
  .map(e => [e.dataset.testid || e.textContent.trim().slice(0,8), e.getBoundingClientRect()])
  .filter(([, r]) => r.width && (r.width < 44 || r.height < 44))

// V2／V3：有沒有容器被內容撐破
[...document.querySelectorAll('*')]
  .filter(e => e.scrollHeight > e.clientHeight + 1 || e.scrollWidth > e.clientWidth + 1)
  .filter(e => getComputedStyle(e).overflow === 'visible' || getComputedStyle(e).overflow === 'hidden')
```

要清掉資料重跑：F12 → Application → Storage → Clear site data。

### 2. 手機真機（R 組真機手感、V8 的 iPhone SE）

```bash
npm run dev -- --host    # 印出區網網址，例如 http://10.0.0.198:5173
```

手機要與這台電腦在同一個 Wi-Fi。**iOS 在非 HTTPS 下不給 `navigator.clipboard`
也不給「加到主畫面」的完整 PWA 行為**，所以 I6／I7（複製）與 R5（standalone）
要等有 HTTPS 才驗得準——最快的方式是部署一份到 Vercel／Netlify／Cloudflare
Pages，或用 `npx localtunnel --port 5173` 之類的工具開一條 HTTPS 通道。

### 3. Google 登入與同步（G 組、I 組）

Google Cloud Console 的完整設定步驟寫在 `.env.example` 開頭。重點是：建立 OAuth
用戶端 ID（網頁應用程式），「已授權的 JavaScript 來源」加入 `http://localhost` 與
`http://localhost:5173`。不需要重新導向 URI，也不需要用戶端密碼。然後：

```bash
cp .env.example .env.local
# 把 VITE_GOOGLE_CLIENT_ID= 填上剛才拿到的 ID
npm run dev
```

瀏覽器要開 `http://localhost:5173`，不要開 `127.0.0.1`，否則 Google 會回
`401 invalid_client`（no registered origin）。設定之後 App 會先停在登入頁，按登入
會跳出 Google 授權視窗，要求讀寫 Google 試算表的權限（G1 驗的就是這件事）。
同意畫面還在「測試中」時，只有列在「測試使用者」裡的帳號能登入，而且每 7 天要重新同意一次。

**I 組的邀請流程需要第二支手機或另一個 Google 帳號**：第一支登入後從配置頁開
邀請面板拿連結，第二支開那條連結。QR 直接用第二支手機的相機掃。

### 4. 已經自動化、不用手動再驗的

```bash
npx vitest run              # 1094 條，含 src/acceptance 的 §15 驗收套件
npx vitest run src/acceptance   # 只跑驗收套件，58 條
npx playwright test         # 手勢引擎的真實瀏覽器測試
```

`src/acceptance/README.md` 有 §15.1 每一條對到哪個檔案的表，跑過的就不用再手動驗。

### 5. 元件展示櫃（dev 限定）

```
http://localhost:5173/?debug=icons     # 15 個圖示
http://localhost:5173/?debug=mantou    # 饅頭三種變體與呼吸動畫
```

---

## 真機手感（模擬器與桌機瀏覽器都不準）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| M1 | 手指在日常頁左右滑動切換月份 | 內容即時跟手，位移約為手指的 55%，放開後吸附，中途無延遲感 | §15.3-33、§10 #7 | 待驗 |
| M2 | 分類卡左滑超過刪除鈕寬度後繼續滑 | 越界後阻力變大而不是硬止，放手回彈自然 | §15.3-34、§10 #15 | 待驗 |
| M3 | 面板下滑關閉，滑到一半放手 | 未過門檻回彈、過門檻關閉，門檻不會誤觸 | §15.3-35、§10 #22/#35 | 待驗 |

## Plan 03 已自動化的部分（不需手動）

手勢引擎的門檻、跟手比例、阻尼、點擊判定、`touch-action` 都有真實瀏覽器測試
（`e2e/gesture.spec.ts`，8 條）。上面 M1–M3 驗的是「手感」，不是數值。

## Plan 04 起跳過的互動測試

（實作到哪就往下加，格式同上）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| D1 | 在把手上往上滑，手指停在半路不放 | 月曆高度即時跟著手指縮短、透明度同步變淡，沒有動畫延遲的黏滯感 | §4 把手、§10 #13 | 待驗 |
| D2 | 往上滑超過 138px 後放手 | 月曆吸附收起，明細補滿剩下的空間 | §4 把手 | 待驗 |
| D3 | 只往上滑約 50px 就放手 | 月曆彈回展開狀態，不是留在半途 | §4 把手 | 待驗 |
| D4 | 快速往上輕彈（位移不到 138px 但很快） | 速度超過 0.4px/ms 一樣吸附收起 | §4 把手 | 待驗 |
| D5 | 在把手上點一下（幾乎沒有位移） | 直接切換收起／展開，不會被當成拖曳 | §4 把手 | 待驗 |
| D6 | 月曆收起後再往下拉 | 月曆跟手回來，高度不會超過 460px 的上限 | §4 把手 | 待驗 |
| D7 | 明細捲到中段後收起月曆，再展開 | 明細的捲動位置維持原樣，不會跳回頂端 | §4 把手、§10 #13 | 待驗 |
| D8 | 觀察把手的提示文字 | 展開時是「往上滑看更多明細」，收起後變成「往下拉看月曆」 | §4 把手 | 待驗 |
| D9 | 點月曆上不同日期 | 選中框是一塊獨立滑塊平滑移動過去，不是每格各自變色閃現 | §4 月曆、§10 #6 | 待驗 |
| D10 | 點不同日期後看日期標題列的金額 | 金額從舊值跑到新值（約 480ms），不是瞬間換掉 | §4 日期標題列、§10 #12 | 待驗 |
| D11 | 切換月份後看收支三卡 | 三個數字都重新 count-up，不是直接跳值 | §4 收支三卡、§10 #31 | 待驗 |
| D12 | 開啟系統的「減少動態效果」後重複 D9–D11 | 所有數字直接落在終值、滑塊不做位移動畫，但功能都正常 | §10 減少動態 | 待驗 |
| D13 | 看右下懸浮 ＋ 的外觀 | 54×54、圓角 19px、紫色漸層、頂部白高光與底部暗面，**沒有**投射陰影 | §2、§10 #28 | 待驗 |
| D14 | 按住懸浮 ＋ 不放，再把手指滑開才鬆手 | 按下時縮到 .92，滑開後回彈，不會卡在按下的樣子也不會誤觸新增 | §10 #28 | 待驗 |
| D15 | 捲動明細時看懸浮 ＋ | ＋ 固定在右下不隨內容移動，且只有日常頁有 | §4 | 待驗 |
| D16 | 看底部分頁列的玻璃感 | 底下的明細內容真的透在玻璃後面（不是一塊不透明淺色），頂部有一條白高光 | §2、§10 #29 | 待驗 |
| D17 | 切換三個分頁 | 白色滑塊平滑位移過去，不是每個頁籤各自變底色 | §2 | 待驗 |
| D18 | 在月曆上左右滑 | 超過 10px 才接管，月曆跟手位移約手指的 55%、透明度降到 .5；點日期格不會被攔掉 | §10 #7 | 待驗 |
| D19 | 在月曆上斜著滑（垂直為主） | 垂直位移超出水平 18px 就放棄手勢，改成正常捲動 | §10 #7 | 待驗 |
| D20 | 換月後看月曆與三卡 | 整區從來的方向滑入（下個月自右、上個月自左），約 420ms | §10 #7 | 待驗 |
| D21 | 點年月選擇器的 ‹ › 翻年 | 12 個月方格整區水平滑動（±38px、opacity .35→1），年份 pill 不跟著滑 | §10 #34 | 待驗 |
| D22 | 點日常頁標題 | ▾ 轉 180° 而不是換成 ▴；面板由下往上 10px 滑入 | §10 #33 | 待驗 |
| D23 | 對照原型看結餘卡的饅頭 | 大小與位置跟原型一致（目前取 30px、疊在卡片右上角），並且在呼吸 | §2、§10 #30 | 待驗 |
| D24 | 明細已經捲到中段時往下拉 | 正常往上捲動內容，**不會**觸發下拉重整 | §10 #27 | 待驗 |
| D25 | 明細捲到頂後繼續往下拉 | 饅頭跟著手指下來（上限 64px、有阻尼），放手轉一圈後回彈 | §10 #27 | 待驗 |
| D26 | 下拉重整後立刻上下捲動明細 | 捲動完全正常，沒有被手勢卡住 | §10 #27 | 待驗 |
| D27 | 切換三個分頁 | 新頁自方向側滑入（往後自右、往前自左，約 420ms），選中頁籤的饅頭壓扁再彈回 | §10 #8、#10 | 待驗 |
| D28 | 點月曆某天後看明細 | 每一列由下往上浮現，逐張差 35ms，第 8 張之後不再加長 | §10 #5 | 待驗 |

## Plan 05 起跳過的互動測試

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| E1 | 點懸浮 ＋ | 面板由下往上滑進來（340ms），遮罩同步淡入 | §10 #1 | 待驗 |
| E2 | 按 ✕ 或儲存 | 面板下滑離場（260ms）後才消失，不是瞬間不見 | §10 #2 | 待驗 |
| E3 | 拖面板把手往下，停在半路不放 | 面板即時跟手往下，不會往上超過原位 | §10 #35 | 待驗 |
| E4 | 把手往下拖超過 110px 放手 | 面板關閉；不到門檻則彈回原位 | §10 #35 | 待驗 |
| E5 | 按數字鍵不放 | 鍵下壓 3px、縮 .96、陰影壓平、底色轉淡紫，鬆手回彈 | §10 #3 | 待驗 |
| E6 | 按儲存鍵不放 | 下壓 4px、陰影壓平，但**不**縮放 | §10 #3 | 待驗 |
| E7 | 看 ⌫ 鍵的配色 | 淡紫底 #EDE9FA + 深紫圖示 #6B57B8，不是灰色 | §5 | 待驗 |
| E8 | 切換支出／收入 | 滑塊平滑位移，底色由紫轉粉，按鈕本身不各自變底色 | §10 #36 | 待驗 |
| E9 | 點日期欄 | ▾ 轉 180°、欄位套紫色外框，小月曆由下往上 10px 滑入 | §10 #38 | 待驗 |
| E10 | ~~點分類欄~~ | 已取消：分類改回原型一直展開，不再有收合列（使用者裁決，取代增補檔 B-3） | — | 不適用 |
| E11 | 在 iPhone SE 375×667 上開記一筆面板 | 整片面板可以順暢捲到數字鍵盤與儲存鍵；分類一直展開（使用者裁決，取代 B-3 的「收起態放得下」） | 使用者裁決 | 待驗 |
| E12 | 存一筆之後看月曆 | 該天的金額跳一下（scale 1→1.12→1，300ms），其他格子不動 | §10 #4 | 待驗 |
| E13 | 存一筆之後看明細 | 新那筆由下往上浮現（320ms） | §10 #4、#5 | 待驗 |
| E14 | 編輯模式按垃圾桶 | 遮罩淡入、確認窗 scale .94→1 彈出，顯示該筆分類·子分類與金額 | §10 #37 | 待驗 |
| E15 | 確認刪除 | 面板下滑關閉，該筆從明細移除時播高度收合（260ms）；iOS 16 以下若不支援 grid-template-rows 動畫，應直接消失而不是卡住 | §10 #37 | 待驗 |
| E16 | 就地新增分類後看分類管理頁 | 新分類排在清單最上方，且立即可選 | §5、§7-2 | 待驗 |

## Plan 06 起跳過的互動測試

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| S1 | 切換週／月／年 | 白色滑塊平滑位移（420ms），文字顏色淡轉，按鈕本身不各自變底色 | §10 #32 | 待驗 |
| S2 | 切換維度後看三個數字 | 結餘、收入、支出都從**前一維度的值**跑到新值，不是從 0 重跑 | §10 #11 | 待驗 |
| S3 | 進入統計頁 | 三個數字從 0 跑上來 | §10 #11 | 待驗 |
| S4 | 切換維度後看折線 | 支出線由左往右描出來（600ms），不是整條瞬間出現 | §10 #11 | 待驗 |
| S5 | 看折線的樣式 | 支出是實線 #9484CE、收入是綠色虛線 #7DB89D（使用者要求，原型是粉色）、支出下方有淡色面積、最後一點較大 | §6 | 待驗 |
| S6 | 進入統計頁看預算條 | 由左往右填充（600ms），每條差 50ms 依序進場 | §10 #12 | 待驗 |
| S7 | 讓某個分類超支後看該條 | 條轉橘 #E08A72、填滿後閃一次、下方顯示「超支 $X」 | §10 #12、§6 | 待驗 |
| S8 | 讓某個分類用到 90% | 條轉黃 #F2C97A | §6 | 待驗 |
| S9 | 開啟系統的「減少動態效果」後重複 S2、S4、S6 | 數字直接落終值、折線直接出現、預算條直接到位，功能都正常 | §10 減少動態 | 待驗 |
| S10 | 在日常頁點某一天再切到統計頁 | 期間是那一天所在的週／月／年，不是今天所在的 | §6 | 待驗 |

## Plan 07 起跳過的互動測試

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| C1 | 點配置頁的「編輯分類與月預算」 | 子頁自右側 38px 滑入（420ms），按 ‹ 反方向返回 | §10 #14 | 待驗 |
| C2 | 在分類卡上左滑，停在半路不放 | 卡片即時跟手，拖曳中無延遲感 | §10 #15 | 待驗 |
| C3 | 左滑超過 84px 繼續拖 | 越界後阻力變大（×0.3）而不是硬止 | §10 #15 | 待驗 |
| C4 | 左滑超過 42px 放手 | 吸附露出刪除鍵；不到門檻則彈回（380ms） | §10 #15 | 待驗 |
| C5 | 卡片已滑開後往右拖 | **跟手 1:1 回到原位**，不該有多餘阻尼（這是 shiftBounds 要解的問題） | §10 #15 | 待驗 |
| C6 | 在分類卡上小幅度左右移動（<10px）後放開 | 卡內的圖示鍵／分類名／金額 pill 仍收得到點擊 | §7.2 | 待驗 |
| C7 | 按刪除鍵並確認 | 分類卡 opacity→0 + 高度收合 260ms 之後才消失，不是瞬間不見 | §10 #16 | 待驗 |
| C8 | 點子頁右上 ＋ | 新卡從清單最上方 translateY -10px→0 浮現（320ms），名稱欄自動聚焦，**不**展開圖示選擇器 | §10 #17 | 待驗 |
| C9 | 點分類卡左側圖示 | 面板由下往上 10px 滑入（240ms），選中項外框 2px 淡入（120ms） | §10 #18 | 待驗 |
| C10 | 換圖示後看日常頁明細與統計頁預算條 | 三處的圖示同步換掉 | §7.2 | 待驗 |
| C11 | 點分類名或金額 pill | 寬度 morph 成輸入欄（200ms）並自動聚焦；✓ 之後 scale 1→1.06→1（240ms） | §10 #19 | 待驗 |
| C12 | 切換分類子頁的支出／收入 | 白滑塊位移，清單整區自方向側滑入（±38px、420ms） | 增補檔 B-1 | 待驗 |
| C13 | 點兩個開關 | knob 位移 220ms，軌道底色同時轉色 | §10 #21 | 待驗 |
| C14 | 刪掉一個用過的分類，再看統計頁與明細 | 已記的帳都還在，仍以原分類名稱顯示 | §7.2 | 待驗 |

## Plan 08 起跳過的互動測試（多數需要真實 Google 帳號）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| G1 | 設好 VITE_GOOGLE_CLIENT_ID 後首次登入 | 跳出 Google 小視窗（不是整頁導走）；因為 App 還在「測試中」會先看到「未經 Google 驗證」；授權畫面列出「所有試算表」與「只限此 App 使用的雲端硬碟檔案」兩項 | 裁決 4、5 | 待驗 |
| G2 | 授權完成後看雲端硬碟 | 出現「加拿大共用記帳」，內有紀錄／配置／年報表／圖表四張工作表 | §9、§14.3 | 待驗 |
| G3 | 看紀錄頁的標頭 | A–N 共 14 欄，順序與增補檔 C-3 一致 | C-3 | 待驗 |
| G4 | 記一筆之後看紀錄頁 | 新增一列，金額欄是**數字**（靠右對齊、可被 SUM 加總），不是文字 | §14.5 | 待驗 |
| G5 | 看年報表頁 | SUMIFS 有算出數字，且與 App 統計頁的月支出一致 | §9 | 待驗 |
| G6 | 開飛航模式記三筆，再開網路 | 三筆都上去，且**沒有重複列** | §14.6 | 待驗 |
| G7 | 兩支手機同時編輯同一筆，各改成不同金額 | 後改的那筆勝出，兩邊最後顯示一致 | §14.6 | 待驗 |
| G8 | 同步進行中看配置頁的狀態點 | 綠點換成圓環轉圈（900ms/圈），完成後縮回圓點、文字變「已同步 · 剛剛」 | §10 #26 | 待驗 |
| G9 | 對方新增一筆 | 跳出 toast 由上滑入，6 秒後自動消失 | §10 #25 | 待驗 |
| G10 | 在 Sheet 上手動加一列（不填 id） | App 同步後忽略那一列，不會出現一筆改不動的帳 | §14.4 | 待驗 |
| G11 | 登入後放著超過一小時 | 同步狀態變成「點一下連線 Google」；點一下就恢復同步，這段期間本機記的帳不會遺失、恢復後會推上去 | 裁決 4（token model 沒有續期） | 待驗 |
| G12 | 不設定 VITE_GOOGLE_CLIENT_ID 直接開 App | 純本機模式正常運作，不跳登入也不報錯 | 實作決定 | 待驗 |

## Plan 09 起跳過的互動測試（登入與邀請，需要真實 Google 帳號與第二支手機）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| I1 | 設好 VITE_GOOGLE_CLIENT_ID 後首次開 App | 停在登入頁，不會先閃一下日常頁再跳走 | §8 | 待驗 |
| I2 | 在登入頁按「使用 Google 登入」 | 彈出 Google 視窗；完成後直接進日常頁，雲端硬碟多一份「加拿大共用記帳」；再登入一次不會多建第二份 | §8 | 待驗 |
| I3 | 登入過後關掉分頁再開 | 直接進日常頁（不用再登入），同步狀態顯示「點一下連線 Google」，點一下就開始同步 | 裁決 4 | 待驗 |
| I4 | 從配置頁開邀請面板 | 面板自底部滑入 420ms，背景遮罩同時淡入 | §10 #22 | 待驗 |
| I5 | 在邀請面板把手上往下拖 | 面板跟手；超過門檻放手才關，不到就彈回 | §10 #22 | 待驗 |
| I6 | 點「複製邀請連結」 | 按鈕底色在 180ms 內轉綠、文字變「已複製連結」，2.2 秒後自己變回來 | §10 #23 | 待驗 |
| I7 | 複製後到訊息軟體貼上 | 貼出來的就是面板上顯示的那條完整連結 | §8.2 | 待驗 |
| I8 | 點「顯示 QR Code」 | QR 區塊由下 10px 滑入並淡入（240ms） | §10 #24 | 待驗 |
| I9 | 用另一支手機掃那個 QR | 掃出來的網址與面板上的連結一字不差，且能打開接受邀請頁 | §8.2 | 待驗 |
| I10 | 在 iOS Safari／Android Chrome 點「用訊息傳送」 | 跳出系統分享單 | §8.2 | 待驗 |
| I11 | 在分享單上按取消 | 什麼都不發生——**不**該跳出「已複製」，也不該顯示失敗訊息 | 實作決定 | 待驗 |
| I12 | 在桌機 Firefox（無 Web Share）點「用訊息傳送」 | 退回複製，並顯示「這個裝置不支援分享，已改為複製連結」 | 實作決定 | 待驗 |
| I13 | 點「預覽她點開後看到的畫面 ›」 | 看到接受邀請頁的樣子，且**不會**真的把自己加進去 | §8.2 | 待驗 |
| I14 | 她在桌機或 Android 直接點邀請連結 | 接受邀請頁；按「用 Google 登入並加入」彈出 Google 視窗，完成後進日常頁，看得到你的分類與帳 | §8.1 | 待驗 |
| I15 | 已登入但尚未加入的帳號開邀請連結 | 直接顯示「加入這本帳」，按下後進日常頁並看得到對方已記的帳 | §8.1 | 待驗 |
| I16 | 已經是成員的帳號再開一次同一條連結 | 顯示「你已經在這本帳裡了」並提供進入按鈕，不會重複加入 | §8.1 | 待驗 |
| I17 | 把連結的 t 參數改掉一個字元再開 | 顯示連結無效，不進入加入流程 | §8.1 | 待驗 |
| I18 | 用一條七天前產生的連結開啟 | 顯示連結已過期，請對方重新產生 | §8.1 | 待驗 |
| I19 | 加入後回到第一支手機記一筆 | 第二支手機同步後看得到，且頭像／成員名稱正確 | §8、§14.6 | 待驗 |
| I20 | ~~在配置頁登出後重開 App~~ | 目前沒有登出功能（token 只在記憶體，關掉 App 就失效），需要時再加 | — | 未實作 |
| I21 | 開邀請面板（還沒分享過）→ 輸入她的 Google 帳號，按「分享帳本」 | 分享前連結的位置只有虛線佔位；分享後收成一行「✓ 已分享給…」，下面才出現邀請連結與複製按鈕；她的信箱收到 Google 的共用通知；試算表的共用對象裡看得到她（可編輯） | 裁決 6、擁有者指示（先分享再傳連結） | 待驗 |
| I22 | 她在 iPhone Safari 開連結 → 加到主畫面 → 從主畫面打開 → 登入頁點「我收到了邀請連結」貼上 → 登入 | 進日常頁，看得到你的分類與帳（主畫面 App 與 Safari 不共用儲存空間，所以要在主畫面 App 裡貼） | 實作決定 | 待驗 |
| I23 | 還沒按「分享帳本」就讓她加入 | 接受邀請頁顯示「還讀不到這本帳。請對方…按『分享帳本』」，不會進入主程式 | 實作決定 | 待驗 |
| I24 | 兩支手機都開著 App，你記一筆；她刪掉一筆 | 對方那邊約 5 秒內出現／消失 | 使用者要求（幾秒內同步） | 待驗 |
| I25 | 她把 App 切到背景，你記一筆，她再切回來 | 一回到前景就同步出現，不必等 5 秒 | 實作決定 | 待驗 |
| I26 | 開發環境（`npm run dev`）清掉網站資料後第一次登入 | 雲端硬碟新建的試算表叫「加拿大共用記帳（開發）」，配置頁 M1 是「環境」、M2 是 `dev` | 擁有者指示（開發／正式分開） | 待驗 |
| I27 | 把開發版的邀請連結貼進正式版的登入頁加入（或反過來） | 接受邀請頁說明這是另一個環境的帳本、不能加入；不會進主程式，分類也沒有被換掉 | 擁有者指示（開發／正式分開） | 待驗 |
| I28 | 連線 Google 後，一小時內重整頁面、切到別的 App 再回來、關掉再開 | 頂端不會出現「點一下連線 Google」，同步照常；超過一小時才需要點一下重新連線 | 使用者要求（不要頻繁登入） | 待驗 |
| I29 | 邀請面板按「預覽對方點開後看到的畫面」 | 進到接受邀請頁，頂端標「預覽」，看到的是邀請卡而不是「你已在這本帳裡」；按「結束預覽」回主程式，不會加入或改動任何東西 | 使用者回報預覽壞掉 | 待驗 |
| I30 | 配置頁點成員 → 改名稱、換饅頭顏色；另一支手機開著 App | 頂端頭像、明細頭像、記一筆「誰記的」都換成新的名稱與顏色；另一支約 5 秒內跟著變；試算表配置頁 N–P 欄看得到兩列 | 使用者要求（稱謂自訂、存雲端） | 待驗 |
| I31 | 她用邀請連結加入後記一筆 | 記一筆「誰記的」直接是她的名稱（預設雪雪大人），沒有切換鈕；你那邊明細的頭像是她的顏色 | 使用者要求（誰記的跟著使用者） | 待驗 |
| I32 | 全新裝置按「用 Google 建立我的帳本」 | 先出現確認卡，提醒對方建好的話改用邀請連結；按「用 Google 建立帳本」才跳 Google 視窗；雲端硬碟多一份帳本 | 裁決 8 | 待驗 |
| I33 | 你換一支手機（或清掉網站資料）再按建立 | 不會多建一本：接回原本那份試算表，看得到之前的帳與分類 | 裁決 8 | 待驗 |
| I34 | 她原本自己有帳本、記過幾筆，再用你的邀請連結加入 | 邀請頁提醒會改記這一本；加入後只看得到你那本的帳；你的試算表裡沒有出現她原本的紀錄（那些仍在她自己的試算表） | 裁決 8 | 待驗 |
| I35 | 分享帳本給對方後回到配置頁；再換一支手機登入同一個帳號看配置頁 | 「邀請成員」不見了，受邀那一列顯示對方的 Google 帳號，兩支手機都一樣；受邀那一方的手機上沒有「邀請成員」 | 使用者要求（邀請過後按鈕消失） | 待驗 |
| I36 | 配置頁點受邀那一列 →「移除這位成員」→「確定移除」，再邀請另一個帳號 | 試算表的共用對象裡原本的人被拿掉，對方的 App 讀不到這本帳；受邀者那一列消失、「邀請成員」回來；邀請新的人後照常加入 | 使用者要求（已邀請的人怎麼換） | 待驗 |
| I37 | 打開 https://could562073.github.io/JointBooks/ | 登入頁正常顯示、字型與饅頭正常；按建立帳本能跳出 Google 視窗（要先在 Cloud Console 加上這個來源） | 部署 | 待驗 |
| I38 | 用正式網址的邀請面板產生連結，另一支手機直接打開 | 網址是 /JointBooks/join?…，進到接受邀請頁，不是 GitHub 的 404 頁 | 部署 | 待驗 |
| I39 | iPhone Safari 打開正式網址 → 加到主畫面 → 從主畫面開 | 以 App 樣式（沒有網址列）開在 /JointBooks/，名稱是「饅頭共享記帳」 | 部署 | 待驗 |

## §15 驗收：自動化跑不到的部分

§15.1 的 A 類「功能行為」1–22 已改用 Vitest 從 `<App />` 跑（`src/acceptance/`，
對照表在 `src/acceptance/README.md`）。以下是留給人工的。

### A 類剩下的版面項目（jsdom 的 getBoundingClientRect 一律回 0，量不出來）

每個斷點都要看：**390×844、430×932、375×667**（第三個是增補檔 B-3 加的）。

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| V1 | 量所有可點元素的實際寬高 | 月曆格、chip、✓／✕、圖示選擇器每格都 ≥44px | §15.1-23 | 待驗 |
| V2 | 對每張卡片量 scrollHeight／scrollWidth | 都 ≤ clientHeight／clientWidth + 1，沒有任何容器被內容撐破 | §15.1-24 | 待驗 |
| V3 | 特別看分類卡 | 子分類 chip 沒有被欄方向 flex 壓縮而裁掉（這條曾經真的壞過） | §15.1-24 | 待驗 |
| V4 | 看所有文字節點的右緣 | 都沒有超出容器右緣 | §15.1-25 | 待驗 |
| V5 | 分類子頁捲動後量標題列與風格列的 top | 數值不變 | §15.1-26 | 待驗 |
| V6 | 日常頁捲動明細後量月曆與月份列的 top | 數值不變 | §15.1-27 | 待驗 |
| V7 | 捲動後量底部分頁列與懸浮 ＋ | 位置不變 | §15.1-28 | 待驗 |
| V8 | iPhone SE 375×667 開記一筆 | 面板可捲到鍵盤最底，捲動時把手與遮罩不跟著跑；不要求一屏放得下（B-3 的條件已被使用者裁決取代） | 使用者裁決 | 待驗 |
| V9 | 日常頁快速上下捲明細 | 只有明細在動，月曆與月份列完全不動；整頁沒有第二條捲軸 | §15.1-1 | 待驗 |

### B 類：截圖比對（需人工看一眼）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| P1 | 對照原型逐頁截圖 13 張：日常（展開／收起）、統計（週月年）、配置、分類子頁、記一筆、編輯、邀請面板、登入、接受邀請 | 顏色、圓角、間距、字級與原型一致 | §15.2-29 | 待驗 |
| P2 | 每條 MOTION 取起／中／終三格 | 位移方向與距離符合 `docs/MOTION.md` 的規格 | §15.2-30 | 待驗 |
| P3 | 390 與 430 各出一份 | 同一版面在兩個寬度都不破 | §15.2-31 | 待驗 |
| P4 | 開 `prefers-reduced-motion: reduce` 再截一次 | 動畫降級但版面完全不變 | §15.2-32 | 待驗 |

### C 類：真機手感（模擬器與桌機瀏覽器都不準）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| R1 | 月曆把手上下拖 | 跟手無延遲、阻尼自然、吸附門檻不會誤觸 | §15.3-33 | 待驗 |
| R2 | 月曆左右滑換月 | 同上；且與點日期不衝突（10px 門檻是否夠） | §15.3-33、34 | 待驗 |
| R3 | 分類卡左滑 | 同上 | §15.3-33 | 待驗 |
| R4 | 明細垂直捲動時稍微帶點橫向 | 不會誤觸左右換月 | §15.3-35 | 待驗 |
| R5 | iOS Safari 加到主畫面後開 | 底部安全區正確、鍵盤彈出時面板不被遮、整頁沒有橡皮筋 | §15.3-36 | 待驗 |
| R6 | standalone 模式下走一次 Google 登入 | 授權視窗關掉後回到同一個 App 畫面，同步狀態變成已同步，不會停在 Safari 另一個分頁 | §15.3-37 | 待驗 |
| R7 | 真機看底部分頁列與面板的毛玻璃 | 觀感與原型一致 | §15.3-38 | 待驗 |
| R8 | 記一筆、切分頁、刪除各觸發一次觸覺回饋 | 強度不過重 | §15.3-39 | 待驗 |
