# 需要手動驗證的項目

自 2026-09-09 起，開發以功能進度為主：**只寫單元測試**，互動／瀏覽器層面不好自動化的
就跳過，改列在這裡讓擁有者手動驗。

每一列都寫清楚「怎麼操作」與「該看到什麼」，才不會變成一句無法執行的空話。

狀態：`待驗` / `通過` / `有問題`

## 真機手感（模擬器與桌機瀏覽器都不準）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
| M1 | 手指在日常頁左右滑動切換月份 | 內容即時跟手，位移約為手指的 55%，放開後吸附，中途無延遲感 | §15.3-33、§10 #7 | 待驗 |
| M2 | 分類卡左滑超過刪除鈕寬度後繼續滑 | 越界後阻力變大而不是硬止，放手回彈自然 | §15.3-34、§10 #15 | 待驗 |
| M3 | 面板下滑關閉，滑到一半放手 | 未過門檻回彈、過門檻關閉，門檻不會誤觸 | §15.3-35、§10 #22/#35 | 待驗 |

## Plan 03 已自動化的部分（不需手動）

手勢引擎的門檻、跟手比例、阻尼、點擊判定、`touch-action` 都有真實瀏覽器測試
（`e2e/gesture.spec.ts`，8 條）。上面 M1–M3 驗的是「手感」，不是數值。

## Plan 04 起跳過的互動測試

（實作到哪就往下加，格式同上）

| # | 怎麼操作 | 該看到什麼 | 來源 | 狀態 |
| --- | --- | --- | --- | --- |
