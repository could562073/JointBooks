# 一筆帳裡的稅 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 記一筆時可以多填「其中稅」一格，讓帳對得上加拿大收據；金額欄仍是收據總額，所有統計不受影響。

**Architecture:** `Txn` 多一個選填的 `taxCents`，Google Sheet 紀錄頁多第 15 欄 O「稅」。記一筆面板在金額下面固定一張白卡，用現有的「實扣 CAD」那套機制（同一組鍵盤、`EntryDraft.field` 切焦點）。稅只寫入與顯示，不進任何加總——`actualCadCents` 仍是統計唯一的來源。

**Tech Stack:** React 19 + TypeScript + CSS Modules、Zustand、Dexie/IndexedDB、Google Sheets API、Vitest + Testing Library、Playwright。

**Spec:** `docs/superpowers/specs/2026-09-22-tax-field-design.md`

## Global Constraints

- 金額一律用**整數分**（`number`）在程式裡流動，字串只在畫面與 Sheet 上出現。
- `actualCadCents` 是所有統計的唯一來源，這次**一行都不准改**；統計頁、預算、年報表、圖表都不在這個計畫的範圍內。
- 稅是**選填**。沒填時 `Txn` 上沒有 `taxCents`（或為 `undefined`），Sheet 的 O 欄寫空字串 `''`，不是 `0.00`。
- 稅與 `amountCents` **同一個幣別**。外幣的「實際扣款 CAD」不拆稅。
- 面板文案逐字照抄：欄位標籤「其中稅（可不填）」、推算行「稅前 $45.87」的前綴是「稅前 」、錯誤訊息「稅不能大於金額」。
- 顏色只准用 `src/styles/tokens.css` 的 token。錯誤字用 `var(--c-expense)`，次要說明字用 `var(--c-text-2)`。
- TypeScript 是 `strict` + `noUncheckedIndexedAccess` + `noUnusedLocals`。陣列索引一律當成可能 `undefined`。
- 每個任務結束前都要跑 `npx tsc -b --noEmit` 與 `npx vitest run`，全綠才 commit。
- 所有指令跑前景，不要開背景 job。
- commit 訊息用英文祈使句，結尾加上 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`。

---

### Task 1: `Txn.taxCents` 與紀錄頁的 O 欄

**Files:**
- Modify: `src/domain/types.ts:31-56`（`Txn`）
- Modify: `src/sheets/rows.ts:7-13`（`TXN_HEADER`）、`:55-72`（`txnToRow`）、`:78-110`（`rowToTxn`）
- Test: `src/sheets/rows.test.ts`

**Interfaces:**
- Consumes: 無（這是第一個任務）
- Produces:
  - `Txn.taxCents?: number`
  - `TXN_HEADER` 變成 15 個元素，`TXN_HEADER[14] === '稅'`
  - `txnToRow(t: Txn): string[]` 回傳 15 格
  - `rowToTxn(row: readonly string[]): Txn | null`：`row[14]` 空白或缺就不放 `taxCents`

- [ ] **Step 1: 改寫既有的欄序測試（會失敗）**

`src/sheets/rows.test.ts` 最上面那個 `describe('紀錄頁欄序（增補檔 C-3）')` 整段換掉。**注意**：原本寫的是「A–N 共 14 欄」，不改的話這個任務一定紅：

```ts
describe('紀錄頁欄序（增補檔 C-3，外加 O 欄「稅」）', () => {
  it('A–O 共 15 欄', () => {
    expect(TXN_HEADER).toHaveLength(15);
    expect(txnToRow(txn())).toHaveLength(15);
  });

  it('欄位對到正確的位置', () => {
    const r = txnToRow(txn());
    expect(r[0]).toBe('2026-09-06');       // A 日期
    expect(r[1]).toBe('外食');              // B 主分類
    expect(r[4]).toBe('CAD');              // E 幣別
    expect(r[8]).toBe('tx-1');             // I id
    expect(r[10]).toBe('c-food');          // K 主分類ID
    expect(r[12]).toBe('FALSE');           // M deleted
  });

  // 稅接在最後面而不是插在金額旁邊：舊版 App 寫的是 A:N，插在中間會讓它把別的欄位寫錯位
  it('稅是最後一欄', () => {
    expect(TXN_HEADER[14]).toBe('稅');
  });
});
```

- [ ] **Step 2: 補稅的讀寫測試（會失敗）**

同檔案，加在 `describe('txnToRow')` 的最後一個 `it` 之後：

```ts
  it('有稅時寫成兩位小數，和其他金額同一個格式', () => {
    expect(txnToRow(txn({ taxCents: 285 }))[14]).toBe('2.85');
  });

  // 空字串而不是 0.00：Sheet 上一眼看得出哪幾筆有稅，也和加這一欄之前的舊列長得一樣
  it('沒填稅時寫空字串', () => {
    expect(txnToRow(txn())[14]).toBe('');
  });

  it('稅是 0 也當成沒填', () => {
    expect(txnToRow(txn({ taxCents: 0 }))[14]).toBe('');
  });
```

加在 `describe('rowToTxn')` 裡：

```ts
  it('讀得回稅', () => {
    const r = txnToRow(txn({ taxCents: 285 }));
    expect(rowToTxn(r)!.taxCents).toBe(285);
  });

  // 加這一欄之前寫下的列只有 14 格，不能因此冒出一個 taxCents: 0
  it('只有 14 欄的舊列讀回來沒有稅', () => {
    const r = txnToRow(txn()).slice(0, 14);
    expect('taxCents' in rowToTxn(r)!).toBe(false);
  });

  it('稅欄空白的列也沒有稅', () => {
    const r = txnToRow(txn());
    r[14] = '';
    expect('taxCents' in rowToTxn(r)!).toBe(false);
  });
```

- [ ] **Step 3: 跑測試，確認紅**

Run: `npx vitest run src/sheets/rows.test.ts`
Expected: FAIL。`TXN_HEADER` 只有 14 個、`r[14]` 是 `undefined`。（vitest 不做型別檢查，紅的是斷言不是編譯。）

- [ ] **Step 4: `Txn` 加欄位**

`src/domain/types.ts`，在 `actualCadCents` 後面插進去：

```ts
  /** 實際扣款 CAD，整數分。所有統計一律用這個欄位 */
  actualCadCents: number;
  /**
   * 這筆金額裡屬於稅的部分（GST、PST／HST、押金、環保費全部加起來），整數分，
   * 幣別同 amountCents。只為了對收據，不參與任何統計；沒填就沒有這個欄位。
   */
  taxCents?: number;
```

- [ ] **Step 5: `rows.ts` 讀寫 O 欄**

標頭尾端加一格：

```ts
export const TXN_HEADER = [
  '日期', '主分類', '子分類', '金額', '幣別', '實扣 CAD', '記帳人', '備註',
  'id', 'updatedAt', '主分類ID', '子分類ID', 'deleted', 'createdAt', '稅',
] as const;
```

`txnToRow` 的回傳陣列最後加一格（`t.createdAt` 之後）：

```ts
    t.createdAt,
    // 沒填或 0 都寫空字串：Sheet 上留白比一整排 0.00 好對帳
    t.taxCents ? amount(t.taxCents) : '',
  ];
```

`rowToTxn` 在 `return` 之前算出來，再用條件展開（和 `Category.updatedAt` 同一個寫法）：

```ts
  const taxRaw = (row[14] ?? '').trim();
  const taxCents = taxRaw ? parseAmount(taxRaw) : 0;

  return {
    id,
    ...
    createdAt: row[13] || (row[9] ?? ''),
    ...(taxCents > 0 ? { taxCents } : {}),
  };
```

- [ ] **Step 6: 跑測試，確認綠**

Run: `npx vitest run src/sheets/rows.test.ts && npx tsc -b --noEmit`
Expected: PASS，型別無誤。

- [ ] **Step 7: 跑全部單元測試**

Run: `npx vitest run`
Expected: 全綠。若有別的測試在數欄位數量，一併更新成 15。

- [ ] **Step 8: Commit**

```bash
git add src/domain/types.ts src/sheets/rows.ts src/sheets/rows.test.ts
git commit -m "feat(sheets): carry an optional tax amount in column O

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 同步與建帳本的範圍改成 A–O

**Files:**
- Modify: `src/sync/syncEngine.ts:9`（`TXN_RANGE`）、`:77`（單列更新範圍）
- Modify: `src/sheets/ledgerSheet.ts:86`（`createLedger` 的標頭範圍）
- Test: `src/sync/syncEngine.test.ts`、`src/sheets/ledgerSheet.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `TXN_HEADER`（15 欄）、`txnToRow`
- Produces: `TXN_RANGE === '紀錄!A2:O'`；單列更新用 `紀錄!A{n}:O{n}`

- [ ] **Step 1: 改既有測試 + 加新測試（會失敗）**

`src/sync/syncEngine.test.ts` 裡把現有這一行的期待值改掉：

```ts
    // 第 1 列是標頭，b 是資料第 2 筆 → 第 3 列
    expect(s.updated[0]!.range).toBe('紀錄!A3:O3');
```

再加一個新的 `it`，放在同一個 describe 裡：

```ts
  // 少了 O 欄，稅推上去會被切掉；多一欄也不能，會蓋到右邊的空白
  it('推送用的範圍含稅欄', async () => {
    expect(TXN_RANGE).toBe('紀錄!A2:O');
    const s = setup({ local: [txn('a', '2026-09-06T12:00:00.000Z')] });
    await s.engine.syncOnce();
    expect(s.client.append).toHaveBeenCalledWith('SID', '紀錄!A2:O', expect.anything());
  });
```

`src/sheets/ledgerSheet.test.ts` 加一個：

```ts
  it('紀錄頁的標頭寫到 O 欄', async () => {
    const f = fakeClient();
    await createLedger(f.client, CATS, 2026, 'dev');
    const header = f.updates.find((u) => u.range.startsWith('紀錄!A1'))!;
    expect(header.range).toBe('紀錄!A1:O1');
    expect(header.rows[0]).toHaveLength(15);
  });
```

`fakeClient()`、`f.updates`、`CATS` 都是這個檔案最上面已經有的，直接用。

- [ ] **Step 2: 跑測試，確認紅**

Run: `npx vitest run src/sync/syncEngine.test.ts src/sheets/ledgerSheet.test.ts`
Expected: FAIL，範圍還是 `A2:N` / `A3:N3` / `A1:N1`。

- [ ] **Step 3: 改三個範圍**

`src/sync/syncEngine.ts`：

```ts
/** 紀錄頁的資料範圍。第 1 列是標頭，所以從第 2 列起算；O 欄是稅 */
export const TXN_RANGE = '紀錄!A2:O';
```

```ts
          await deps.client.update(sid, `紀錄!A${line}:O${line}`, [txnToRow(t)]);
```

`src/sheets/ledgerSheet.ts`：

```ts
  await client.update(id, `${SHEET.txns}!A1:O1`, [[...TXN_HEADER]]);
```

- [ ] **Step 4: 跑測試，確認綠**

Run: `npx vitest run && npx tsc -b --noEmit`
Expected: 全綠。

- [ ] **Step 5: Commit**

```bash
git add src/sync/syncEngine.ts src/sync/syncEngine.test.ts src/sheets/ledgerSheet.ts src/sheets/ledgerSheet.test.ts
git commit -m "feat(sync): widen the record range to include the tax column

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 舊帳本補寫 O1 的標頭

現有的帳本是加這一欄之前建的，O1 是空的。App 在第一次推送之前補寫一次。

**Files:**
- Create: `src/sync/taxHeader.ts`
- Create: `src/sync/taxHeader.test.ts`
- Modify: `src/sync/syncEngine.ts:11-19`（`SyncDeps`）、`:72-79`（推送迴圈前）
- Modify: `src/sync/controller.ts:26-43`（`SyncControllerDeps`）、`:59-66`（建 engine）
- Modify: `src/App.tsx:406-430`（`createSyncController(...)` 的 deps）
- Test: `src/sync/syncEngine.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `TXN_HEADER`；`SHEET` from `src/sheets/ledgerSheet.ts`；`ledgerRepo.getMeta` / `setMeta`
- Produces:
  - `TAX_HEADER_KEY = 'taxHeaderSid'`
  - `ensureTaxHeader(client: SheetsClient, sid: string): Promise<void>`
  - `SyncDeps.ensureTaxHeader(sid: string): Promise<void>`（**必填**）
  - `SyncControllerDeps.ensureTaxHeader?(sid: string): Promise<void>`（選填，不給就什麼都不做）

- [ ] **Step 1: 寫 `taxHeader` 的失敗測試**

Create `src/sync/taxHeader.test.ts`：

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { resetDb } from '../db/schema';
import { ledgerRepo } from '../repo/ledgerRepo';
import type { SheetsClient } from '../sheets/client';
import { ensureTaxHeader, TAX_HEADER_KEY } from './taxHeader';

function client() {
  return { update: vi.fn(async () => ({})) } as unknown as SheetsClient;
}

beforeEach(async () => { await resetDb(); });

describe('ensureTaxHeader', () => {
  it('第一次會把稅寫進 O1，並記住是哪一本帳', async () => {
    const c = client();
    await ensureTaxHeader(c, 'S1');
    expect(c.update).toHaveBeenCalledWith('S1', '紀錄!O1', [['稅']]);
    expect(await ledgerRepo.getMeta(TAX_HEADER_KEY)).toBe('S1');
  });

  it('同一本帳第二次什麼都不做', async () => {
    const c = client();
    await ensureTaxHeader(c, 'S1');
    await ensureTaxHeader(c, 'S1');
    expect(c.update).toHaveBeenCalledTimes(1);
  });

  // 登出後接到另一本舊帳本時，那一本的 O1 也還是空的：記布林值就永遠補不到
  it('換一本帳就再補一次', async () => {
    const c = client();
    await ensureTaxHeader(c, 'S1');
    await ensureTaxHeader(c, 'S2');
    expect(c.update).toHaveBeenCalledTimes(2);
    expect(await ledgerRepo.getMeta(TAX_HEADER_KEY)).toBe('S2');
  });
});
```

- [ ] **Step 2: 跑測試，確認紅**

Run: `npx vitest run src/sync/taxHeader.test.ts`
Expected: FAIL，找不到 `./taxHeader`。

- [ ] **Step 3: 寫 `taxHeader.ts`**

Create `src/sync/taxHeader.ts`：

```ts
import { ledgerRepo } from '../repo/ledgerRepo';
import type { SheetsClient } from '../sheets/client';
import { SHEET } from '../sheets/ledgerSheet';
import { TXN_HEADER } from '../sheets/rows';

/**
 * 已經補過稅欄標頭的帳本 id。存的是 sid 而不是布林值：一台手機可能登出後接到
 * 另一本舊帳本，布林旗標會讓第二本永遠補不到標頭。
 */
export const TAX_HEADER_KEY = 'taxHeaderSid';

/**
 * 補寫紀錄頁 O1 的「稅」標頭。加這一欄之前建的帳本那一格是空的，資料寫得進去
 * 但人在 Sheet 上看不出那一欄是什麼。每台裝置、每本帳只打一次 API。
 *
 * 剛建好的帳本其實已經有標頭（createLedger 寫的是 A1:O1），這裡仍會多寫一次
 * 同樣的值。刻意不去 createLedger 的兩個呼叫點各設一次旗標：那是兩個會被忘記
 * 的地方，一整本帳多打一次 API 換掉這個風險划算。
 */
export async function ensureTaxHeader(client: SheetsClient, sid: string): Promise<void> {
  if (await ledgerRepo.getMeta<string>(TAX_HEADER_KEY) === sid) return;
  await client.update(sid, `${SHEET.txns}!O1`, [[TXN_HEADER[14]]]);
  await ledgerRepo.setMeta(TAX_HEADER_KEY, sid);
}
```

- [ ] **Step 4: 跑測試，確認綠**

Run: `npx vitest run src/sync/taxHeader.test.ts`
Expected: PASS。

- [ ] **Step 5: 寫 engine 會呼叫它的失敗測試**

`src/sync/syncEngine.test.ts` 的 `setup()` 裡多收一個紀錄用的陣列，並補上新的必填 dep。在 `const states: string[] = [];` 下面加：

```ts
  const headered: string[] = [];
```

在 `createSyncEngine({ ... })` 的 deps 裡加一行：

```ts
    ensureTaxHeader: async (sid) => { headered.push(sid); },
```

`return` 也要帶出去：

```ts
  return { engine, client, appended, updated, saved, states, headered };
```

新增測試：

```ts
describe('稅欄標頭', () => {
  it('有東西要推時，推之前先補標頭', async () => {
    const s = setup({ local: [txn('a', '2026-09-06T12:00:00.000Z')] });
    await s.engine.syncOnce();
    expect(s.headered).toEqual(['SID']);
  });

  // 只是輪詢拉資料的那幾十次不該為了一格標頭多打 API
  it('沒東西要推時不補', async () => {
    const s = setup();
    await s.engine.syncOnce();
    expect(s.headered).toEqual([]);
  });
});
```

- [ ] **Step 6: 跑測試，確認紅**

Run: `npx vitest run src/sync/syncEngine.test.ts`
Expected: FAIL，`s.headered` 是空陣列——engine 還沒有人叫它。

- [ ] **Step 7: engine 接上**

`src/sync/syncEngine.ts` 的 `SyncDeps` 加一個欄位：

```ts
  isOnline(): boolean;
  onState(s: SyncState): void;
  /** 第一次推送前補寫紀錄頁的稅欄標頭；已經補過就什麼都不做 */
  ensureTaxHeader(sid: string): Promise<void>;
};
```

推送迴圈前面加一行：

```ts
      // 舊帳本的 O1 是空的。只有真的要推東西才補，免得每 5 秒輪詢都多打一次 API
      if (toPush.length > 0) await deps.ensureTaxHeader(sid);

      for (const t of toPush) {
```

- [ ] **Step 8: controller 與 App 接上**

`src/sync/controller.ts` 的 `SyncControllerDeps` 加：

```ts
  /** 補寫紀錄頁的稅欄標頭；不給就不補（測試與舊呼叫點） */
  ensureTaxHeader?(sid: string): Promise<void>;
```

`createSyncEngine({ ... })` 加：

```ts
    ensureTaxHeader: d.ensureTaxHeader ?? (async () => {}),
```

`src/App.tsx:406` 的 `createSyncController({ ... })`，在 `spreadsheetId` 那一行後面插一行
（`cloud` 是同一個 effect 裡已經有的變數）：

```ts
      spreadsheetId: () => sidNow,
      // 舊帳本的紀錄頁沒有稅欄的標頭，第一次推送前補一次
      ensureTaxHeader: (sid) => ensureTaxHeader(cloud.client, sid),
```

檔案頂端補 import：

```ts
import { ensureTaxHeader } from './sync/taxHeader';
```

- [ ] **Step 9: 跑全部測試**

Run: `npx vitest run && npx tsc -b --noEmit`
Expected: 全綠。controller 的既有測試沒給 `ensureTaxHeader`，靠預設值過。

- [ ] **Step 10: Commit**

```bash
git add src/sync/taxHeader.ts src/sync/taxHeader.test.ts src/sync/syncEngine.ts src/sync/syncEngine.test.ts src/sync/controller.ts src/App.tsx
git commit -m "feat(sync): backfill the tax column header on existing ledgers

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 草稿與儲存路徑帶得動稅

**Files:**
- Modify: `src/repo/ledgerRepo.ts:6-15`（`NewTxnInput`）
- Modify: `src/screens/entry/entryDraft.ts`（`AmountField`、`EntryDraft`、`draftForNew`、`draftFromTxn`、`setCurrency`、`canSave`、`toInput`，並新增 `taxCents`、`preTaxCents`、`taxError`）
- Test: `src/screens/entry/entryDraft.test.ts`、`src/repo/ledgerRepo.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `Txn.taxCents`
- Produces:
  - `type AmountField = 'amount' | 'cad' | 'tax'`
  - `EntryDraft.tax: string`
  - `NewTxnInput.taxCents?: number`
  - `taxCents(d: EntryDraft): number`
  - `preTaxCents(d: EntryDraft): number | null`（沒填稅、金額是 0、或稅 > 金額時回 `null`）
  - `taxError(d: EntryDraft): string | null`
  - `toInput(d)` 回傳的物件一定含 `taxCents` 這個鍵（沒填時是 `undefined`，這樣編輯時才清得掉）

- [ ] **Step 1: 寫失敗測試（entryDraft）**

`src/screens/entry/entryDraft.test.ts`，import 補上新函式：

```ts
import {
  actualCadCents, canSave, draftForNew, draftFromTxn, needsCadField, preTaxCents,
  setCurrency, setKind, setMain, taxCents, taxError, toInput,
} from './entryDraft';
```

加一整個新的 describe（放在檔案最後）：

```ts
describe('其中稅', () => {
  const withTax = (tax: string, amount = '48.72') => ({ ...NEW(), amount, tax });

  it('新增模式預設沒有稅', () => {
    expect(NEW().tax).toBe('');
    expect(taxCents(NEW())).toBe(0);
  });

  it('稅前是金額減稅', () => {
    expect(preTaxCents(withTax('2.85'))).toBe(4_587);
  });

  it('沒填稅時沒有稅前可以顯示', () => {
    expect(preTaxCents(withTax(''))).toBeNull();
  });

  it('金額還沒打時也沒有稅前', () => {
    expect(preTaxCents(withTax('2.85', ''))).toBeNull();
  });

  it('稅剛好等於金額是合法的（整筆都是押金之類）', () => {
    const d = withTax('48.72');
    expect(taxError(d)).toBeNull();
    expect(canSave(d)).toBe(true);
    expect(preTaxCents(d)).toBe(0);
  });

  it('稅大於金額：擋下儲存並給訊息', () => {
    const d = withTax('50.00');
    expect(taxError(d)).toBe('稅不能大於金額');
    expect(canSave(d)).toBe(false);
  });

  // 先點稅欄、金額還沒打時亮紅字只是噪音；存不存得了本來就由金額 0 那條規則決定
  it('金額還沒打時不報稅的錯，但也還是存不了', () => {
    const d = withTax('2.85', '');
    expect(taxError(d)).toBeNull();
    expect(canSave(d)).toBe(false);
  });

  it('toInput 有稅時帶整數分', () => {
    expect(toInput(withTax('2.85')).taxCents).toBe(285);
  });

  // 編輯一筆本來有稅的帳、把稅刪掉時，patch 一定要含這個鍵，否則舊值會留著
  it('toInput 沒稅時帶 undefined，而不是漏掉這個鍵', () => {
    const i = toInput(withTax(''));
    expect('taxCents' in i).toBe(true);
    expect(i.taxCents).toBeUndefined();
  });

  it('編輯模式把稅帶回字串', () => {
    expect(draftFromTxn(CATS, txn({ taxCents: 285 })).tax).toBe('2.85');
  });

  it('沒有稅的舊帳帶回空字串，不是 0', () => {
    expect(draftFromTxn(CATS, txn()).tax).toBe('');
  });

  // 換幣別本來就要重打金額，稅留著讓使用者自己改比清掉少一次意外
  it('換幣別不清掉稅', () => {
    expect(setCurrency(withTax('2.85'), 'USD').tax).toBe('2.85');
    expect(setCurrency({ ...withTax('2.85'), currency: 'USD' }, 'CAD').tax).toBe('2.85');
  });

  it('切回 CAD 時焦點在稅欄就留在稅欄，只有停在實扣欄才收回金額', () => {
    const onTax = { ...withTax('2.85'), currency: 'USD' as const, field: 'tax' as const };
    expect(setCurrency(onTax, 'CAD').field).toBe('tax');
    const onCad = { ...withTax('2.85'), currency: 'USD' as const, field: 'cad' as const };
    expect(setCurrency(onCad, 'CAD').field).toBe('amount');
  });
});
```

`txn()` 這個 helper 目前定義在 `describe('draftFromTxn')` 裡面。把它整個搬到檔案最上層（`const NEW = ...` 下面），兩個 describe 共用，不要複製一份。

- [ ] **Step 2: 寫失敗測試（ledgerRepo）**

`src/repo/ledgerRepo.test.ts` 加：

檔案頂端補一個型別 import：

```ts
import { ledgerRepo, type NewTxnInput } from './ledgerRepo';
```

（原本是 `import { ledgerRepo } from './ledgerRepo';`。）新的 describe 放在檔案最後：

```ts
describe('稅', () => {
  // cat() 與 cats 是這個檔案 beforeEach 已經準備好的，直接用
  const input = (over: Partial<NewTxnInput> = {}): NewTxnInput => {
    const c = cat('外食');
    return {
      date: '2026-09-05', mainId: c.id, subId: c.subs[0]!.id,
      amountCents: 4_872, currency: 'CAD', actualCadCents: 4_872, by: '我', note: '',
      ...over,
    };
  };

  it('新增時存得進去', async () => {
    const t = await ledgerRepo.addTxn(input({ taxCents: 285 }));
    expect(t!.taxCents).toBe(285);
    expect((await ledgerRepo.listTxns())[0]!.taxCents).toBe(285);
  });

  it('編輯時改得掉', async () => {
    const t = await ledgerRepo.addTxn(input({ taxCents: 285 }));
    const next = await ledgerRepo.updateTxn(t!.id, { taxCents: 100 });
    expect(next.taxCents).toBe(100);
  });

  // 編輯一筆本來有稅的帳、把稅刪掉：patch 帶的是 undefined，舊值不能留著
  it('編輯時清得掉', async () => {
    const t = await ledgerRepo.addTxn(input({ taxCents: 285 }));
    const next = await ledgerRepo.updateTxn(t!.id, { taxCents: undefined });
    expect(next.taxCents).toBeUndefined();
  });
});
```

- [ ] **Step 3: 跑測試，確認紅**

Run: `npx vitest run src/screens/entry/entryDraft.test.ts src/repo/ledgerRepo.test.ts`
Expected: FAIL，`preTaxCents` 與 `taxError` 匯入不到（`TypeError: ... is not a function`）。

- [ ] **Step 4: `NewTxnInput` 加欄位**

`src/repo/ledgerRepo.ts`：

```ts
export type NewTxnInput = {
  date: string;
  mainId: string;
  subId: string;
  amountCents: number;
  currency: Currency;
  actualCadCents: number;
  by: Person;
  note: string;
  /** 金額裡屬於稅的部分，整數分，幣別同 currency。沒有就是 undefined */
  taxCents?: number;
};
```

`addTxn` 與 `updateTxn` 不用改：兩者都是把 `input`／`patch` 整包展開進去，`taxCents` 會自己跟著走，`undefined` 也會正確地把舊值蓋掉。

- [ ] **Step 5: `entryDraft.ts` 加稅**

型別：

```ts
/** 數字鍵盤打在哪一個欄位上（§5：這些欄位共用同一組鍵盤） */
export type AmountField = 'amount' | 'cad' | 'tax';

export type EntryDraft = {
  kind: CategoryKind;
  /** 原幣金額的顯示字串 */
  amount: string;
  /** 實扣 CAD 的顯示字串；currency === 'CAD' 時用不到 */
  cad: string;
  /** 其中稅的顯示字串，可不填。跟 amount 同一個幣別 */
  tax: string;
  ...
};
```

`draftForNew` 的回傳加 `tax: ''`：

```ts
    amount: '', cad: '', tax: '',
```

`draftFromTxn` 加：

```ts
    // 沒有稅的舊帳帶成空字串，不要憑空生一個 0
    tax: t.taxCents ? centsToInput(t.taxCents) : '',
```

`setCurrency` 改成不動稅、只在焦點停在實扣欄時才收回金額欄：

```ts
/**
 * 換幣別。切回 CAD 時把實扣欄位清掉——CAD 模式下它不存在，焦點留在上面會變成
 * 打字沒有任何反應。稅不清：稅跟著金額的幣別走，換幣別本來就要重打金額，
 * 稅留著讓使用者自己改比清掉少一次意外。
 */
export function setCurrency(d: EntryDraft, currency: Currency): EntryDraft {
  if (currency === 'CAD') {
    return { ...d, currency, cad: '', field: d.field === 'cad' ? 'amount' : d.field };
  }
  return { ...d, currency };
}
```

新增三個純函式（放在 `actualCadCents` 後面）：

```ts
/** 其中稅的分。沒填就是 0 */
export function taxCents(d: EntryDraft): number {
  return toCents(d.tax);
}

/**
 * 扣掉稅之後的金額，給面板顯示用，不存。沒填稅、金額還沒打、或稅比金額還大時
 * 回 null：前兩種情況顯示「稅前 $0.00」只是噪音，第三種則是錯誤狀態，
 * 這時候不論顯示負數還是絕對值都沒有意義（旁邊已經有一行紅字在講了）。
 */
export function preTaxCents(d: EntryDraft): number | null {
  const amount = toCents(d.amount);
  const tax = taxCents(d);
  if (amount === 0 || tax === 0 || tax > amount) return null;
  return amount - tax;
}

/**
 * 稅欄的錯誤訊息；沒問題回 null。
 *
 * 金額還沒打時一律不報錯：使用者可能先點稅欄，這時候亮「稅不能大於金額」是
 * 噪音不是資訊。儲存還是會被擋下來——canSave 本來就不讓金額 0 的帳寫入。
 */
export function taxError(d: EntryDraft): string | null {
  const amount = toCents(d.amount);
  if (amount === 0) return null;
  return taxCents(d) > amount ? '稅不能大於金額' : null;
}
```

`canSave` 多一個條件：

```ts
export function canSave(d: EntryDraft): boolean {
  if (!d.mainId || !d.subId) return false;
  if (toCents(d.amount) === 0) return false;
  if (taxError(d)) return false;
  return actualCadCents(d) !== 0;
}
```

`toInput` 帶稅：

```ts
export function toInput(d: EntryDraft): NewTxnInput {
  return {
    date: d.date,
    mainId: d.mainId,
    subId: d.subId,
    amountCents: toCents(d.amount),
    currency: d.currency,
    actualCadCents: actualCadCents(d),
    by: d.by,
    note: d.note,
    // 一定要帶這個鍵：編輯時把稅刪掉，patch 少了它舊值就會留著
    taxCents: taxCents(d) || undefined,
  };
}
```

- [ ] **Step 6: 跑測試，確認綠**

Run: `npx vitest run && npx tsc -b --noEmit`
Expected: 全綠。`EntrySheet.tsx` 這時可能因為 `EntryDraft` 多了必填的 `tax` 而報型別錯——它是用 `draftForNew`／`draftFromTxn` 產生草稿的，不該報錯；真的報錯就是有地方自己拼了一個 draft 物件，補上 `tax: ''`。

- [ ] **Step 7: Commit**

```bash
git add src/repo/ledgerRepo.ts src/repo/ledgerRepo.test.ts src/screens/entry/entryDraft.ts src/screens/entry/entryDraft.test.ts
git commit -m "feat(entry): keep a tax amount in the entry draft

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 記一筆面板的「其中稅」卡片

**Files:**
- Modify: `src/screens/entry/EntrySheet.tsx`（`key` 的分派、稅卡片的 JSX）
- Modify: `src/screens/entry/EntrySheet.module.css`
- Test: `src/screens/entry/EntrySheet.test.tsx`

**Interfaces:**
- Consumes: Task 4 的 `taxError`、`preTaxCents`、`AmountField`、`EntryDraft.tax`
- Produces: testid `field-tax`（卡片本身）、`pre-tax`（稅前那行）、`tax-error`（錯誤訊息）

- [ ] **Step 1: 寫失敗測試**

`src/screens/entry/EntrySheet.test.tsx` 加一個 describe：

```ts
describe('其中稅（對收據用）', () => {
  it('卡片一直在，預設是 0 的淡色字，沒有稅前那行', () => {
    render(<EntrySheet {...BASE} />);
    expect(screen.getByTestId('field-tax')).toBeInTheDocument();
    expect(screen.queryByTestId('pre-tax')).not.toBeInTheDocument();
  });

  it('點稅欄之後，數字鍵打進稅欄而不是金額欄', () => {
    render(<EntrySheet {...BASE} />);
    typeAmount('4872');
    fireEvent.click(screen.getByTestId('field-tax'));
    typeAmount('285');
    expect(screen.getByTestId('field-amount')).toHaveTextContent('48.72');
    expect(screen.getByTestId('field-tax')).toHaveTextContent('2.85');
  });

  it('填了稅就顯示稅前', () => {
    render(<EntrySheet {...BASE} />);
    typeAmount('4872');
    fireEvent.click(screen.getByTestId('field-tax'));
    typeAmount('285');
    expect(screen.getByTestId('pre-tax')).toHaveTextContent('稅前 $45.87');
  });

  it('稅大於金額：出現訊息，儲存鍵按不下去', () => {
    const onSave = vi.fn();
    render(<EntrySheet {...BASE} onSave={onSave} />);
    typeAmount('1000');
    fireEvent.click(screen.getByTestId('field-tax'));
    typeAmount('2000');
    expect(screen.getByTestId('tax-error')).toHaveTextContent('稅不能大於金額');
    fireEvent.click(screen.getByTestId('key-save'));
    expect(onSave).not.toHaveBeenCalled();
  });

  it('存出去的內容帶著稅', () => {
    const onSave = vi.fn();
    render(<EntrySheet {...BASE} onSave={onSave} />);
    typeAmount('4872');
    fireEvent.click(screen.getByTestId('field-tax'));
    typeAmount('285');
    fireEvent.click(screen.getByTestId('key-save'));
    expect(onSave).toHaveBeenCalledWith(expect.objectContaining({ amountCents: 4_872, taxCents: 285 }));
  });

  it('編輯一筆有稅的帳，稅帶進來', () => {
    render(<EntrySheet {...BASE} txn={txn({ amountCents: 4_872, actualCadCents: 4_872, taxCents: 285 })} />);
    expect(screen.getByTestId('field-tax')).toHaveTextContent('2.85');
    expect(screen.getByTestId('pre-tax')).toHaveTextContent('稅前 $45.87');
  });
});
```

`typeAmount` 這個 helper 檔案裡已經有了，直接用。

- [ ] **Step 2: 跑測試，確認紅**

Run: `npx vitest run src/screens/entry/EntrySheet.test.tsx`
Expected: FAIL，找不到 `field-tax`。

- [ ] **Step 3: `EntrySheet.tsx` 的鍵盤分派與卡片**

import 補上：

```ts
import {
  actualCadCents, canSave, draftForNew, draftFromTxn, needsCadField, preTaxCents,
  setCurrency, setKind, setMain, taxError, toInput, type AmountField, type EntryDraft,
} from './entryDraft';
```

`key` 改成三路分派（原本是 `cad` 與 `amount` 二選一）：

```ts
  const key = useCallback((k: KeypadKey) => {
    const c = keyChar(k);
    setDraft((d) => {
      if (d.field === 'cad') return { ...d, cad: pushDigit(d.cad, c) };
      if (d.field === 'tax') return { ...d, tax: pushDigit(d.tax, c) };
      return { ...d, amount: pushDigit(d.amount, c) };
    });
  }, []);
```

在 `saveable` 旁邊算兩個值：

```ts
  const saveable = canSave(draft);
  const preTax = preTaxCents(draft);
  const taxErr = taxError(draft);
```

JSX 插在實扣 CAD 面板（`{cadPanel.mounted && (...)}` 那一段）之後、`<CategoryPicker ...>` 之前：

```tsx
        {/* 對收據用：金額裡有多少是稅。統計一律用金額，不碰這一格 */}
        <button
          type="button"
          className={styles.tax}
          data-focused={draft.field === 'tax' ? '' : undefined}
          onClick={() => focus('tax')}
          data-testid="field-tax"
        >
          <span className={styles.taxTop}>
            <span className={styles.cardLabel}>其中稅（可不填）</span>
            <span className={styles.taxLine}>
              <span className={styles.dollarSm} aria-hidden="true">$</span>
              <span className={styles.cadDigits} data-empty={isBlank(draft.tax) ? '' : undefined}>
                {draft.tax || '0'}
              </span>
            </span>
          </span>
          {preTax !== null && (
            <span className={styles.preTax} data-testid="pre-tax">
              稅前 {formatCad(preTax, 'none')}
            </span>
          )}
        </button>

        {taxErr && (
          <p className={styles.taxError} data-testid="tax-error">{taxErr}</p>
        )}
```

- [ ] **Step 4: CSS**

`src/screens/entry/EntrySheet.module.css`，加在 `.cadDigits[data-empty]` 那一行後面：

```css
/* 其中稅：跟實扣 CAD 同一種白卡，但標籤和數字並排，下面一行是算出來的稅前 */
.tax {
  display: block;
  width: 100%;
  margin-bottom: 12px;
  border: 0;
  border-radius: var(--r-sm);
  background: var(--c-card);
  padding: 10px 13px;
  text-align: left;
  cursor: pointer;
}

.tax[data-focused] {
  box-shadow: inset 0 0 0 2px var(--c-primary);
}

.taxTop {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 8px;
}

/* 和 .cadLine 一樣，但不要它的 margin-top：這裡是並排不是換行 */
.taxLine {
  display: flex;
  align-items: baseline;
  gap: 4px;
}

.preTax {
  display: block;
  margin-top: 2px;
  font-family: var(--f-body);
  font-size: 11.5px;
  color: var(--c-text-2);
}

.taxError {
  margin: -6px 0 10px 10px;
  font-family: var(--f-body);
  font-size: 11.5px;
  color: var(--c-expense);
}
```

- [ ] **Step 5: 跑測試，確認綠**

Run: `npx vitest run && npx tsc -b --noEmit`
Expected: 全綠。

- [ ] **Step 6: Commit**

外觀由使用者自己在 `npm run dev`（5173）上驗收，不要為了確認畫面去截圖或另開 server。

```bash
git add src/screens/entry/EntrySheet.tsx src/screens/entry/EntrySheet.module.css src/screens/entry/EntrySheet.test.tsx
git commit -m "feat(entry): add the tax card to the entry sheet

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: E2E、版本與文件

**Files:**
- Modify: `e2e/entry.spec.ts`
- Modify: `package.json`、`package-lock.json`（版本）
- Modify: `README.md`、`docs/PROGRESS.md`

**Interfaces:**
- Consumes: Task 5 的 testid `field-tax`、`pre-tax`
- Produces: 無（這是最後一個任務）

- [ ] **Step 1: 寫 E2E**

`e2e/entry.spec.ts`，加在「存一筆」那個 test 後面：

```ts
  test('記一筆有稅的帳：稅前算給你看，存完再打開稅還在', async ({ page }) => {
    await openSheet(page);
    await page.getByTestId('key-4').click();
    await page.getByTestId('key-8').click();
    await page.getByTestId('key-.').click();
    await page.getByTestId('key-7').click();
    await page.getByTestId('key-2').click();

    await page.getByTestId('field-tax').click();
    await page.getByTestId('key-2').click();
    await page.getByTestId('key-.').click();
    await page.getByTestId('key-8').click();
    await page.getByTestId('key-5').click();
    await expect(page.getByTestId('pre-tax')).toHaveText('稅前 $45.87');

    await page.getByTestId('key-save').click();
    await expect(page.getByTestId('entry-sheet')).toHaveCount(0, { timeout: 2_000 });

    // 月曆展開時明細那一列在小手機上會落到分頁列底下，先收起來
    await page.getByTestId('calendar-handle').click();
    await settle(page);
    await page.getByTestId('txn-list').locator('li').first().click();
    await expect(page.getByTestId('field-tax')).toContainText('2.85');
    await expect(page.getByTestId('pre-tax')).toHaveText('稅前 $45.87');
  });
```

鍵盤的 testid 是 `key-<鍵>`（`src/screens/entry/Keypad.tsx:52`），小數點就是 `key-.`，退格是 `key-back`。

- [ ] **Step 2: 跑這一支 E2E**

Run: `npx playwright test e2e/entry.spec.ts --project=ip13`
Expected: PASS。第一次跑要等 dev server 起來（它跑的是自己的 5174，不會碰你手上的 5173）。

- [ ] **Step 3: 跑短螢幕那一支，確認鍵盤沒被擠出去**

Run: `npx playwright test e2e/entry.spec.ts --project=se`
Expected: PASS。多了一張卡之後「畫面比面板矮時，鍵盤固定在底部一直看得到」這個 test 仍然要綠——它就是這次最該防的迴歸。

- [ ] **Step 4: 跑完整 E2E**

Run: `npx playwright test`
Expected: 全綠。整輪大約 10 分鐘，前景跑，不要開背景 job。

- [ ] **Step 5: 升版本**

Run: `npm version minor --no-git-tag-version`
Expected: `package.json` 與 `package-lock.json` 變成 `1.3.0`。

**注意**：這個指令會改 lockfile，Vite 會因此重載所有開著的頁面。跑之前先確定沒有 Playwright 正在跑。

- [ ] **Step 6: 更新文件**

`docs/PROGRESS.md` 在最新的條目旁邊加一條，照既有格式寫：一筆帳可以記「其中稅」，金額仍是收據總額，稅只寫進 Sheet 的 O 欄與編輯面板，不進統計。

`README.md` 的功能清單加一行：記帳時可以填「其中稅」，對加拿大收據用。

- [ ] **Step 7: Commit**

```bash
git add e2e/entry.spec.ts package.json package-lock.json README.md docs/PROGRESS.md
git commit -m "chore(release): 1.3.0, record the tax inside a transaction

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```
