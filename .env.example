# Google OAuth 2.0 用戶端 ID（「網頁應用程式」類型）
#
# 在 Google Cloud Console 要做的設定：
#   1. 「API 和服務 → 程式庫」：啟用 Google Sheets API 與 Google Drive API
#   2. 「OAuth 同意畫面」：類型選外部、維持「測試中」，把你和對方的 Google 帳號
#      加進「測試使用者」；範圍加入 .../auth/spreadsheets 與 .../auth/drive.file
#   3. 「憑證 → OAuth 用戶端 ID」：在「已授權的 JavaScript 來源」加入
#      http://localhost、http://localhost:5173，以及正式網址的來源
#      （例如 https://could562073.github.io，只填來源，不含 /JointBooks 路徑）
#      開發時要用 localhost 開，不要用 127.0.0.1 或區網 IP：Google 只認登記過的來源，
#      沒登記會回 401 invalid_client「no registered origin」。存檔後可能要等幾分鐘才生效。
#
# 不需要重新導向 URI，也不需要用戶端密碼：登入走 Google Identity Services 的
# token model，全部在瀏覽器完成。用戶端密碼絕對不要放進這個檔案或任何前端程式碼。
#
# 用法：把這個檔案複製成 .env.local 再填值（.env.local 已被 git 忽略）。
# 沒設定這一項時，App 以純本機模式運作（資料存 IndexedDB，不同步），不會嘗試登入。
VITE_GOOGLE_CLIENT_ID=
