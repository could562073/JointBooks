# Google OAuth 2.0 用戶端 ID（網頁應用程式類型）
#
# 取得方式：Google Cloud Console → API 和服務 → 憑證 → 建立 OAuth 用戶端 ID。
# 已授權的重新導向 URI 要填 <你的網址>/auth/callback，且必須與這裡的 origin 完全一致。
# 只需要 drive.file 範圍，不需要整個雲端硬碟權限（§14.2）。
#
# 沒設定這一項時，App 以純本機模式運作（資料存 IndexedDB，不同步），
# 不會嘗試登入也不會報錯。
VITE_GOOGLE_CLIENT_ID=
